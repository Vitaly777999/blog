<%= form_for([@post, @post.comments.build]) do |f| %>
  <p>
    <%= f.label :Автор %><br>
    <%= f.text_field :commenter %>
  </p>
  <p>
    <%= f.label :Текст %><br>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit :Добавить %>
  </p>
<% end %>